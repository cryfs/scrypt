# Biicode configuration file

[requirements]
	 messmer/cmake: 3

[parent]
	messmer/scrypt: 0
[paths]
    # Local directories to look for headers (within block)
    # /
    # include
    /
    /libcperciva/cpusupport/
    /libcperciva/alg
    /libcperciva/util
    /libcperciva/crypto
    /lib/crypto
    /lib/util
    /lib/scryptenc

[dependencies]
    # Manual adjust file implicit dependencies, add (+), remove (-), or overwrite (=)
    # hello.h + hello_imp.cpp hello_imp2.cpp
    # *.h + *.cpp
    scrypt_platform.h + config_freebsd.h
    * - lib/crypto/crypto_scrypt-ref.c
    libcperciva/cpusupport/cpusupport.h + libcperciva/cpusupport/cpusupport_x86_aesni.c libcperciva/cpusupport/cpusupport_x86_sse2.c

[mains]
    # Manual adjust of files that define an executable
    # !main.cpp  # Do not build executable from this file
    # main2.cpp # Build it (it doesnt have a main() function, but maybe it includes it)
    !main.c
    !libcperciva/cpusupport/Build/cpusupport-X86-AESNI.c
    !libcperciva/cpusupport/Build/cpusupport-X86-CPUID.c
    !libcperciva/cpusupport/Build/cpusupport-X86-SSE2.c

[tests]
    # Manual adjust of files that define a CTest test
    # test/* pattern to evaluate this test/ folder sources like tests

[hooks]
    # These are defined equal to [dependencies],files names matching bii*stage*hook.py
    # will be launched as python scripts at stage = {post_process, clean}
    # CMakeLists.txt + bii/my_post_process1_hook.py bii_clean_hook.py

[includes]
    # Mapping of include patterns to external blocks
    # hello*.h: user3/depblock  # includes will be processed as user3/depblock/hello*.h

[data]
    # Manually define data files dependencies, that will be copied to bin for execution
    # By default they are copied to bin/user/block/... which should be taken into account
    # when loading from disk such data
    # image.cpp + image.jpg  # code should write open("user/block/image.jpg")
    #
    # cpusupport.sh is a script that checks for CPU features and creates a config header with some #define's for them
    * + libcperciva/cpusupport/Build/cpusupport.sh
    * + libcperciva/cpusupport/Build/cpusupport-X86-AESNI.c
    * + libcperciva/cpusupport/Build/cpusupport-X86-CPUID.c
    * + libcperciva/cpusupport/Build/cpusupport-X86-SSE2.c
